<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled state -->
    <item android:color="@color/whiteColor" android:state_enabled="false" />
    <!-- Focused state -->
    <item android:color="@color/whiteColor" android:state_focused="true" />
    <!-- Default state -->
    <item android:color="@color/whiteColor" />
</selector>
